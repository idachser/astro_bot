# План: переход с парсинга astronomy.com на iCal-фид in-the-sky.org

## Цель

Заменить хрупкий скрейпинг HTML-статьи «Sky This Week» на структурированный
источник — iCal-календарь событий in-the-sky.org:

```
https://in-the-sky.org/newscalyear_ical.php?year=<YYYY>&maxdiff=7
```

Фид отдаёт ~500 событий в год (фазы Луны, соединения, противостояния,
покрытия, метеорные потоки, затмения) с точным UTC-временем, заголовком,
описанием и ссылкой. Данные © Dominic Ford, использование — некоммерческое
с обязательной атрибуцией.

## Этап 1. Новый источник данных

- [ ] Добавить зависимость `icalendar` (и убрать `bs4`) в `pyproject.toml`.
- [ ] Написать `services/ics_feed.py`: скачивание ICS за текущий год
      (в декабре — ещё и за следующий), парсинг VEVENT в список словарей
      `{uid, dt_utc, summary, description, url}`.
- [ ] Вынести URL фида в `config.py` (`ICS_FEED_URL`), удалить `PAGE_URL`
      и `AGENTS` / `user_agents.txt` (случайные user-agent больше не нужны).

## Этап 2. Схема БД

- [ ] Новая таблица `events`:
      `uid TEXT PRIMARY KEY, dt_utc TEXT NOT NULL, summary TEXT NOT NULL,
      description TEXT, url TEXT` — вместо строковой даты «Friday, July 3»
      хранить ISO-дату/время.
- [ ] Переписать `db_queries.py` на параметризованные запросы
      (убрать f-string SQL), добавить выборку по диапазону дат:
      `WHERE date(dt_utc) BETWEEN ? AND ?`.
- [ ] `services/events.py`: `sync_events()` (upsert по uid) и
      `get_events(day)` / `get_events(range)` по ISO-датам.
- [ ] Миграция: старые данные не переносить — таблица пересоздаётся,
      фид полностью покрывает текущий год.

## Этап 3. Хендлеры

- [ ] `today` / `yesterday` / `tomorrow`: убрать regex-обрезку нулей и
      `DATE_FORMAT`-строки, запрашивать по `date.today() ± timedelta`.
      В сутки бывает несколько событий — склеивать их в одно сообщение.
- [ ] `week`: листалка `<` / `>` по дням текущей недели (пн–вс) вместо
      «последних 7 строк из БД»; хранить в callback_data сам день, а не
      счётчик в глобальном словаре (заодно уйдёт баг с уходом в минус).
- [ ] `specific_date`: парсить «July 15» в ISO-дату и запрашивать по ней.
- [ ] `autosend_events`: по субботам — `sync_events()` + дайджест событий
      на предстоящую неделю; починить `get_users_ids()` (сейчас
      возвращает только первую строку, рассылка доходит одному юзеру).

## Этап 4. Тексты и атрибуция

- [ ] В `templates.py`: формат сообщения «дата + список событий»,
      время события показывать явно (пока UTC; таймзона юзера — этап 6).
- [ ] Заменить благодарность Astronomy Magazine на атрибуцию
      «Data courtesy of In-The-Sky.org © Dominic Ford» в приветствии
      и в подвале дайджеста (условие лицензии).
- [ ] Обновить README.

## Этап 5. Тесты и уборка

- [ ] Починить импорты в `tests.py` (остались от старого имени пакета).
- [ ] Тесты парсера ICS на сохранённом фикстур-файле (без сети),
      тесты выборок по диапазону дат на `:memory:`-базе.
- [ ] Удалить `services/scrap_data.py`, `parse_data.py`, `extractor.py`
      и связанные тесты скрейпера.

## Этап 6 (потом, отдельно)

- Показ времени событий в таймзоне пользователя — колонка `timezone`
  уже собирается при /start, но нигде не используется.
- Команда «Image of the day» через NASA APOD (`NASA_TOKEN` уже в конфиге).
- Фильтр событий по видимости с локации пользователя (RSS-фиды
  in-the-sky.org умеют учитывать координаты).

## Риски

- Фид неофициальный, без SLA: при недоступности — оставлять старые данные
  в БД и логировать, бот продолжает отвечать из кеша.
- Описания на английском: типовые события (фазы Луны и т.п.) позже можно
  локализовать шаблонами по ключевым словам summary.
